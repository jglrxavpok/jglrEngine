package org.jge.components.hud;

import org.jge.Window;

public class HUDMenu extends HUDWidget
{

	public HUDMenu()
	{
		super(Window.getCurrent().getRealWidth(), Window.getCurrent().getRealHeight());
	}

}
